1. Объявите переменные типов: Число, число с плавающей точкой, строка, булево значение, нул, массив.
2. Создайте переменную $name, присвойте ей строковое значение содержащее ваше имя. Создайте переменную $age, и присвойте ей числовое значение содержащее ваш возраст.
3. Выведите с помощью echo (print, var_dump) фразу “Меня зовут …..”, выведите фразу “Мой возраст … лет”. Измените код таким образом чтоб каждая фраза была с новой строки.
4. удалите переменную $age
5. Создайте константу и присвойте ей любое значение, выведите ее на экран.
6. Создайте переменную $age и присвойте ей произвольное число, напишите конструкцию if которая выводит фразу “Вам еще работать и работать” если значение переменной $age попадает в диапазон от 18 до 59. Расширьте конструкцию if выводя фразу “Вам пора на пенсию”, если значение переменной больше 59. Далее расширьте конструкцию и выводите фразу “Вам еще рано работать” если значение переменной $age попадает в диапазон от 1 до 17 включительно. И наконец дополните конструкцию таким образом чтоб на экран вывелась фраза “Неизвестный возраст” если значение переменной $age не попадает ни в один диапазон.
7. Создайте переменную $day, присвойте ей любое числовое значение. С помощью конструкции switch выведите фразу “Это рабочий день”, если переменной $day попадает в диапазон чисел от 1 до 5 включительно. Выведите фразу “Это выходной день”, если значение $day равно числам 6 или 7, и на конец выведите фразу “Неизвестный день” если значение $day не попадает в диапазон чисел от 1 до 7 включительно. 
8. Создайте массив $bmw с ячейками “model”, “speed”, “doors”, “year”. Заполните ячейки значениями “X5”, 120, 5, 2006.
Создайте массивы $toyota и $opel аналогичные массиву $bmw и заполните их - $toyota “Carina”, 130, 4, 2007
$opel “Corsa”, 140, 5, 2007
Выведите значение всех трех массивов в виде:
name - model - speed - doors - year
9. Используя цикл for выведите все нечетные числа от 1 до 50. 
10. Используя цикл for заполните массив от 0 до 100


Дополнительные задачи:
   1. Деление числа на разряды
Введите пятизначное число: 10819
1 цифра равна 1
2 цифра равна 0
3 цифра равна 8
4 цифра равна 1
5 цифра равна 9
2.  Перевод в верхний регистр средствами пхп. Создайте переменную со словом в нижнем регистре (пример $string = ‘hello’) выведите на экран фразу в верхнем регистре.
3. Перевод числа из метров в километры. Создайте переменную с числовым значением, и выведите на экран это число конвертированное в километры.
4. Создайте три сайта произвольного названия. У каждого сайта должен быть свой домен. Создайте для каждого конфиг-файлы в АПАЧ
5. Напишите примитивный калькулятор используя условие и три переменных)))))) для особо одаренных, со звездочкой *


19.11.2017
Задания с функциями. Необходимо прочитать о функциях и реализовать их самостоятельно.
   1. strlen()
   2. empty()
   3. trim()
   4. intval()
   5. implode()
   6. explode()
   7. array_merge()
   8. array_unique()


23.11.2017
   1. array_key_exists()
   2. array_keys()
   3. array_combine()
   4. array_search()
   5. in_array()
   6. array_diff()
   7. array_intersect()
   8. sort()
   9. max()* 
   10.  min()*
   11.  round()*
задачи со звездочкой на подумать, для особо одаренных)






27.11.2017
Поэкспериментируйте с отправкой данных на сервер методами пост и гет, как мы делали на занятии, создайте несколько разных страничек с разными формами. Сделайте так чтоб после отправки ваши данные обрабатывались, записались в сессию и выводились на экран, при перезагрузках страничек данные не должны пропадать. Читайте ссылки которые я вам надавал. Добавьте в страничках навигацию по вашим страницам, для перенаправления используйте GET, для форм - POST